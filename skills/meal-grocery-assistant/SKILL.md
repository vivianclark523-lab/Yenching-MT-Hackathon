---
name: meal-grocery-assistant
description: 智能外卖与采购助手。用户说"太忙了帮我点饭"、"随便给我点个不踩雷的"、"不知道吃什么"、"给我几个外卖灵感"、"想吃火锅/麻辣烫/面/快餐帮我选"、"帮我一键下单"、"猫粮/纸/水是不是该补了"时触发；也可由饭点、常点店优惠、天气、日程、采购周期主动触发。核心能力：基于历史外卖、天气、日程、优惠和采购周期生成可确认的一键下单方案，覆盖极度繁忙、纠结灵感、明确品类三类场景。
metadata:
  {
    "openclaw":
      {
        "emoji": "🍱",
        "requires": { "bins": ["python3"] },
      },
  }
---

# 智能外卖与采购助手

饭点和补货场景的主动决策助手：少问、少筛、直接给用户可确认的方案。

> ⚠️ **输出规范**
> 1. 严禁向用户展示技术细节、字段名、JSON、脚本路径、命令
> 2. 只展示用户要决策的信息：店名、菜品、价格、配送、优惠、推荐理由
> 3. 所有真实消费动作必须先给订单预览并等用户明确确认
> 4. 推荐理由必须来自历史订单、评分、优惠、天气、日程或采购周期，不得泛泛说"不错"

> ⚠️ **强制重读**：每次会话第一次触发时必须重新读取本文件

---

## When to Use

- 用户极忙："帮我直接点个饭"、"随便来个不踩雷的"、"我没空选"
- 用户纠结："不知道吃什么"、"给我几个灵感"、"今天吃点啥"
- 用户明确品类："想吃火锅"、"来个麻辣烫"、"帮我找个面"
- 用户想吃某品类又要最优惠/比价："想吃猪脚饭哪家划算"、"哪家便宜"、"用券最优惠的那家"
- 用户要省钱/品质："20 块以内"、"品质好一点"、"别预制菜"
- 用户处于旅游状态，想吃当地特色
- 饭点 + 常点店有优惠、天气影响饮食选择、日程显示明天有相同品类局
- 周期采购："猫粮是不是快没了"、"纸巾/水/驱蚊液帮我补一下"

## When NOT to Use

- 到店排队、取号、出发提醒 → 交给 `watch-restaurant-queues`
- 多节点出行、群聊行程、朋友圈卡片 → 交给 `route-planning-sharing`
- 用户只是闲聊，没有用餐或采购意图
- 医疗、药品、酒精等需要专业资质或年龄校验的购买请求

---

## 风险提示与免责声明

- 外卖和采购数据为 Mock 演示数据，向用户表达为"预计"或"可选方案"
- 不自动支付、不自动提交真实订单；下单前必须二次确认
- 用户忌口、预算、地址以当前对话显式输入优先；`USER.md` / `MEMORY.md` 原生画像和 Mock 外卖数据只是默认值
- 天气、日程、昨日订单用于避坑，不作为强制医疗或健康建议

---

## 场景识别与分流

| 场景 | 判断依据 | 产品形态 |
|---|---|---|
| **极度繁忙** | "没空选"、"直接点"、"帮我定" | 1 个最稳一键下单方案 |
| **纠结灵感** | "不知道吃什么"、"给我几个选择" | 4 张左右滑动推荐卡 |
| **明确品类** | "想吃 X"、"就吃 X" | 常用方案 + 探索方案 |
| **跨店比价** | "想吃 X，哪家划算/最便宜/最优惠用券" | 跨店券后比价表 + 默认推荐 |
| **采购补货** | "猫粮/纸/水快没了" 或周期触发 | 1-3 个补货建议 |

主动触发只发一次软提示。用户无回应则沉默，不追问、不重复打扰。

### 路由优先级

用户不需要说自己属于哪种场景，按自然语言自动分流：

1. 明确采购词（猫粮、纸、水、补货、买东西）→ **采购补货**
2. 明确品类词（火锅、麻辣烫、面、猪脚饭、轻食、炸鸡等）→ **明确品类**；若同时带"哪家划算/最便宜/最优惠/比价/用券省钱"等价格诉求 → **跨店比价**
3. 明确代订词（很忙、直接帮我下单、别问了、随便来个稳的）→ **极度繁忙**
4. 灵感词（今天吃什么、不知道吃什么、再挑一挑、给我几个选择）→ **纠结灵感**
5. 只有"帮我点外卖"且没有品类 → 默认按 **极度繁忙** 给 1 个稳妥方案

每次输出末尾都给用户 2-3 个快捷路由，方便用户一键修正方向：

```text
快捷选项：
1. 我很忙，直接帮我下单外卖
2. 我不满意，还想再挑一挑
3. 我想吃某一品类，你帮我推荐一下
```

快捷路由含义：

| 快捷话术 | 重新路由 |
|---|---|
| 我很忙，直接帮我下单外卖 | `busy` |
| 我不满意，还想再挑一挑 | `inspiration`，复用上一轮约束并换一批 |
| 我想吃某一品类，你帮我推荐一下 | 追问品类；用户补充后走 `category` |

---

## 安全门控

1. **下单前确认**：必须展示店名、菜品、价格、配送、优惠、地址摘要，等用户说"确认/下单/就这个"。
2. **昨日去重**：极忙场景默认排除昨日已点店和同款主菜。
3. **天气避坑**：炎热天气默认避开火锅、羊蝎子等重热品类；寒冷天气可提高热食权重。
4. **日程避重**：已知明天有火锅局，今天不主动推荐火锅，除非用户明确要求。
5. **旅游状态**：灵感卡必须包含当地特色方案。
6. **缺信息策略**：预算缺省用画像预算；地址缺省用画像地址；忌口不明确时避开高风险选项。

---

## 工作流程

### Step 0 · 识别意图和约束

静默抽取：

- 当前场景：`busy` / `inspiration` / `category` / `grocery`
- 用户明确品类、预算、忌口、是否旅游、天气、日程
- 是否要求立即下单

先使用已注入的 `USER.md` / `MEMORY.md` 画像作为默认偏好；再调用 Mock 命令补充 Skill 2 专用的历史订单、复购和采购周期数据：

```bash
python3 skills/meal-grocery-assistant/scripts/meal_context.py profile
```

### Step 1 · 极度繁忙：直接代订

```bash
python3 skills/meal-grocery-assistant/scripts/meal_context.py recommend \
  --scenario busy \
  --weather hot
```

输出只给 top 1：

```text
我给你配一个稳的：
<店名>｜<菜品>｜预计 ¥<价格>
<配送时间> 分钟左右到

理由：<常点/高分/不和昨日重复/天气或日程避坑>
要我按这个下单吗？

快捷选项：
1. 我很忙，直接帮我下单外卖
2. 我不满意，还想再挑一挑
3. 我想吃某一品类，你帮我推荐一下
```

### Step 2 · 纠结灵感：四张推荐卡

```bash
python3 skills/meal-grocery-assistant/scripts/meal_context.py recommend \
  --scenario inspiration \
  --weather hot
```

如果用户在旅游：

```bash
python3 skills/meal-grocery-assistant/scripts/meal_context.py recommend \
  --scenario inspiration \
  --traveling
```

展示 4 张卡片：

```text
给你 4 张卡，左右滑一下就能定：

① 高性价比｜稳妥
<店名>｜<菜品>｜¥<价格>
理由：<...>

② 高性价比｜探索
...

③ 品质优选｜稳妥
...

④ 品质优选｜探索
...

快捷选项：
1. 我很忙，直接帮我下单外卖
2. 我不满意，还想再挑一挑
3. 我想吃某一品类，你帮我推荐一下
```

### Step 3 · 明确品类：常用 + 探索

```bash
python3 skills/meal-grocery-assistant/scripts/meal_context.py recommend \
  --scenario category \
  --category "火锅" \
  --weather hot
```

输出两个方案：

```text
想吃 <品类> 的话，我给你两个：

常用：<店名>｜<菜品>｜¥<价格>
理由：你点过 <N> 次，稳定不踩雷

探索：<店名>｜<菜品>｜¥<价格>
理由：<rating> 分，你没试过，口碑更好/更符合今天状态

选哪个？我再给你确认订单。

快捷选项：
1. 我很忙，直接帮我下单外卖
2. 我不满意，还想再挑一挑
```

### Step 3B · 跨店比价：同一道菜，哪家券后最便宜

用户想吃某品类、且在意价格/优惠（"哪家划算"、"最便宜"、"用券最优惠"）时，把核心菜名作为 `--want` 传入，脚本会跨店把每家"最优呈现（单点/套餐/凑单）"的券后实付比出来：

```bash
python3 skills/meal-grocery-assistant/scripts/meal_context.py deal --want "猪脚饭"
```

`--want` 传**核心品类词**(脚本按品类/菜名匹配)。当前支持跨店比价的品类:**猪脚饭 / 火锅 / 快餐 / 麻辣烫 / 面 / 家常小炒 / 轻食 / 茶饮 / 甜点**。用户口语**先归一到这些词再传**:奶茶/果茶/茶饮店 → `茶饮`;甜品/蛋糕/糕点 → `甜点`;盖饭/小炒/炒菜 → `家常小炒`;沙拉/减脂餐 → `轻食`。匹配不到时脚本返回"没找到",据此让用户换个说法。

> ⚠️ **所有价格、用券、券后实付、是否达标都来自脚本 JSON，严禁自己口算或编造**。你只负责把 JSON 渲染成话术。

读 `data` 渲染：

- `default` = 默认推荐（达标 rating ≥ 4.2 里券后最便宜）
- `comparison` = 各店最优呈现，按券后价排序（店名 / 菜或套餐 / 原价 / 券后 / 用了哪些券 / 是否达标）
- `cheaper_but_risky` = 比默认更便宜但评分未达标的店 —— **必须诚实亮出来让用户自己选，不要藏**
- `quality_upsell` = 达标里更高分的替代（"多花 ¥X 更稳"）
- 每行的 `addon_hint` = 凑单提示（"再凑 ¥X 能上某券、省 ¥Y"）
- 每行的 `wait` = 时间杠杆：`action:"wait"` → "等到 `until`（`wait_minutes` 分钟后）神券激活，能到 `payable`、省 `save`"；`action:"order_now"` → "`before` 后那张券失效/秒空会变贵，建议现在下单"；`null` = 等不等都一样

输出模板：

```text
想吃<品类>，我帮你比了几家券后价：

默认给你：<default.shop_name>｜<presentation>｜券后 <default.payable>（<rating>分）
理由：达标里最便宜，用了 <coupons>

想更稳：<upsell.shop_name>｜券后 <upsell.payable>（<rating>分），多花一点更靠谱
想更省：<risky.shop_name> 券后 <risky.payable>，但只有 <rating> 分有点踩雷，要试吗？

（如某家带 wait）想吃 <shop>：现在 <payable>，再等 <wait_minutes> 分钟到 <until> 的神券能压到 <save> 的差价；
或：<shop> 那张神券 <before> 就秒空了，要的话现在下。

要哪家？定了我再给你确认订单。
```

### Step 4 · 采购补货（周期触发 + 美团采购商家券后比价 + 凑一单）

日用品(猫粮/纸/水…)按购买周期到点提醒,并在**美团采购商家**(小象超市/物美/便利店等,**不越出美团**)里比**券后单价**最优。

```bash
python3 skills/meal-grocery-assistant/scripts/meal_context.py grocery
```

> ⚠️ **所有价格/规格/券后单价/用券都来自脚本 JSON,严禁口算编造**。

读 `data` 渲染:

- `due[]` = 该补的日用品。每条:
  - `best` = 该需求**券后单价最优**方案(`merchant` 商家 / `sku` 规格 / `qty` 件数 / `payable` 券后总价 / `unit_price` **券后单价** / `coupons` 用券);`covers_cycles` = 这个规格能覆盖几个补货周期(>1 即"囤货更划算")
  - `alternatives[]` = 同需求其它规格/商家(按券后单价)
- `cart` = 把这几样**凑进同一个小象超市购物车**,凑满99减20 + 满39包邮(`original→payable`,`saved`,`coupons`,`addon_hint`=再凑多少能上某券)

> 采购券和外卖券的不同要会讲:① **规格/单价**——大包装+满减常常**券后单价更低**(推大包装+说"够你用 N 个周期");② **第二件半价/8折**——建议"买2提更划算"(单价更低);③ **凑一单**——把周期上要补的几样凑到满减/包邮,比分开买省。

输出模板:

```text
这几样该补了,我按券后单价比了下美团上的几家:

🐱 <best.sku>｜<best.merchant>｜券后 <best.payable>（单价 <best.unit_price>，够你用约 <covers_cycles> 个周期）
   比 <alt.sku> 单价更低；用了 <best.coupons>

🧻 <best.sku>×<qty>｜第二件8折更划算，单价 <best.unit_price>

要不要都放小象超市一单？<cart 商品>共 <cart.payable>（满99减20+包邮，省 <cart.saved>）。
```

### Step 5 · 下单确认

用户确认后，只输出订单预览，不伪装真实支付：

```text
下单前确认一下：
<店名/商超>
<菜品/商品>
预计实付 ¥<价格>
送到：<地址摘要>

确认后我再提交。
```

### 主动出击 · 临期券提醒（服务找人，后台触发）

不等用户开口：后台定时（cron / heartbeat）扫描用户钱包里**即将过期**的券，算"怎么用掉它最划算"，主动推一条软提示。

```bash
python3 skills/meal-grocery-assistant/scripts/meal_context.py scan
```

读 `pushes`（**为空则保持沉默、不打扰**）：每条含临期券、过期时刻、`uses_expiring_coupon`，以及 `suggestion`（用掉它的最优凑单：店名 / 菜 / 原价 / 券后 / 省了多少 / 用了哪些券）。

输出模板（仅在有 push 时发、且只发一次）：

```text
你那张「<coupon.description>」今晚就过期了。
按你常点的 <suggestion.presentation> 正好凑到门槛，用掉这张券券后 <suggestion.payable>（省 <suggestion.saved>），
比不用更划算，要我帮你下单吗？
```

> ⚠️ 主动推送只发一次软提示；用户无回应则沉默，不追问、不重复打扰。`scan` 只产出"待发推送"，不自动下单。

---

## 推荐策略

### 极度繁忙

硬过滤：

- 昨日已点店铺 / 昨日同款主菜
- 天气炎热时的重热品类
- 明日日程已安排的同品类
- 评分低于 4.2 或配送超过 45 分钟
- 预制菜风险高

排序：

- 历史常点和高评分优先
- 有当前可用券加分
- 配送更快加分
- 与天气、日程、预算越贴合越靠前

### 纠结灵感

固定输出 4 档：

- 高性价比 + 稳妥快餐
- 高性价比 + 未尝试小吃/当地特色
- 品质优选 + 稳妥正餐
- 品质优选 + 未尝试高分店

旅游状态下，探索卡至少一张必须带"当地特色"。

### 明确品类

- 常用方案：该品类中历史次数最高的店
- 探索方案：该品类中高评分、未尝试、画像匹配的店
- 如果用户明确说"今天就要火锅"，天气和明日日程只作为提醒，不再硬过滤

---

## 与其他 Skill 的联动

- **Skill 1** 等位超过 30 分钟 + 饭点 → 本 Skill 推荐外卖垫一口
- **Skill 3** 行程结束 → 本 Skill 询问是否补买饮品、纸巾、明日早餐等
- 本 Skill 发现用户要到店堂食/排队 → 转给 Skill 1

联动只做一次，不连续追问。

---

## 标准回复格式

| 场景 | 模板 |
|---|---|
| 忙碌代订 | `我给你配一个稳的：\n<店名>｜<菜品>｜预计 ¥<价格>\n理由：<...>\n要我按这个下单吗？` |
| 灵感卡 | `给你 4 张卡，左右滑一下就能定：\n① 高性价比｜稳妥\n...` |
| 明确品类 | `想吃 <品类> 的话，我给你两个：\n常用：...\n探索：...` |
| 跨店比价 | `想吃<品类>，我帮你比了几家券后价：\n默认：<店>｜券后<价>（<分>）\n想更稳：...\n想更省但踩雷：...\n要哪家？` |
| 采购补货 | `这几样该补了，我按券后单价比了几家：\n<商品>｜<商家>｜券后<价>（单价<单价>，够用<N>周期）\n要不要凑小象超市一单：满99减20+包邮，省<X>？` |
| 无可用方案 | `这个条件下我不硬凑。卡住的是：<原因>。要不要放宽 <预算/品类/范围>？` |
| 下单确认 | `下单前确认一下：\n<明细>\n确认后我再提交。` |
| 快捷路由 | `快捷选项：\n1. 我很忙，直接帮我下单外卖\n2. 我不满意，还想再挑一挑\n3. 我想吃某一品类，你帮我推荐一下` |

---

## 命令清单

| 命令 | 用途 |
|---|---|
| `python3 skills/meal-grocery-assistant/scripts/meal_context.py profile` | 汇总 Skill 2 Mock 外卖画像与历史数据 |
| `python3 skills/meal-grocery-assistant/scripts/meal_context.py recommend --scenario busy` | 极忙场景 top 1 |
| `python3 skills/meal-grocery-assistant/scripts/meal_context.py recommend --scenario inspiration` | 纠结场景 4 张卡 |
| `python3 skills/meal-grocery-assistant/scripts/meal_context.py recommend --scenario category --category "<品类>"` | 明确品类常用 + 探索 |
| `python3 skills/meal-grocery-assistant/scripts/meal_context.py deal --want "<品类>"` | 跨店同品类券后比价（默认 O3 达标最便宜，含凑单/时间杠杆/诚实surface踩雷） |
| `python3 skills/meal-grocery-assistant/scripts/meal_context.py scan` | 服务找人：扫临期券 → 给"用掉它的最优凑单"主动推送（无则沉默） |
| `python3 skills/meal-grocery-assistant/scripts/meal_context.py grocery` | 周期采购补货:美团采购商家券后单价比价(规格/第二件折扣)+ 凑一单满减包邮 |

---

## 详细参考文档

- [`references/recommendation-logic.md`](references/recommendation-logic.md) — 三场景推荐策略、打分和演示样例
